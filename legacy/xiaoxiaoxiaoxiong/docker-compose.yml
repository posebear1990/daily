version: '3.8'

services:
  homepage:
    build: .
    container_name: xiaoxiong-homepage
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
